{
  "name": "greif-agentic-demo",
  "version": "1.0.0",
  "description": "Agentic SDLC pipeline demo for Greif - static site clone + CI/CD",
  "private": true,
  "scripts": {
    "test": "vitest run tests",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "playwright": "^1.49.0",
    "vitest": "^2.1.0"
  }
}
